@model ApplicationUser
@{
	Layout = null;
	var dir = Thread.CurrentThread.CurrentCulture.TextInfo.IsRightToLeft ? "rtl" : "ltr";
	var ReturnUrl = string.IsNullOrWhiteSpace(Context.Request.Path) ? "~/" : $"~{Context.Request.Path.Value}{Context.Request.QueryString}";
}
<!doctype html>
<html lang="en" dir="@dir">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="~/fonts/fontawesome/css/all.min.css">
	@if (dir == "rtl")
	{
		<link rel="stylesheet" href="~/vendor/bootstrap/css/bootstrap.rtl.min.css">
	}
	else
	{
		<link rel="stylesheet" href="~/vendor/bootstrap/css/bootstrap.min.css">
	}
	<link rel="stylesheet" href="~/css/style.css">
	<link rel="shortcut icon" href="/img/icon.png" type="image/x-icon">
	<title>@localizer["Forget Password"]</title>
</head>

<body>
	<div class="login-page en">
		<div class="container login-body">
			<div>
				<img class="top" src="~/img/Top Shape.png">
			</div>
			
			<partial name="_Culture"></partial>
			
			<div class="row login-content">
				<div class="col-lg-7 col-md-6 right">
					<img src="~/img/Forgot password-bro.png">
				</div>
				<div class="col-lg-5 col-md-6 col-12 left">
					<div class="head">
						<h2>@localizer["Welcome Back"]</h2>
						<h4>@localizer["Forget yor password"]</h4>
					</div>
					<form method="post" asp-action="ForgetPassword" asp-route-returnUrl="@ViewBag.URL">
						<div class="form">
							<div asp-validation-summary="ModelOnly" class="text-danger"></div>
							<div class="warp-input">
								<input asp-for="Email" class="input" type="text" placeholder="@localizer["email"]">
								<i class="fa-solid fa-envelope input-icon"></i>
								<span asp-validation-for="Email" class="text-danger"></span>
							</div>
							<button type="submit" class="btn-main btn-primaryy">@localizer["Send"]</button>
						</div>
					</form>
				</div>
			</div>
			<img class="bottom" src="~/img/Bottom Shape.png">
		</div>
	</div>
	<script src="~/vendor/jquery/jquery-3.6.0.slim.min.js"></script>
	<script src="~/vendor/bootstrap/js/popper.js"></script>
	<script src="~/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
	<script src="~/js/main.js"></script>

</body>
</html>